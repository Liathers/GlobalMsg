main_config = {
    "settings": {
        "prefix": "~",
        "debugPrefix": ">>",
        "token": "Token",
        "debugToken": "Token",
        "modlog-channel": 000, #needs to be an int
        "debugModlog-channel": 000, #needs to be an int
        "maxchars": 256, #needs to be an int
        "dev-id": 567406113809498113, #needs to be an int
        "debugMode": True #needs to be a boolean
    }
}

config = main_config["settings"]